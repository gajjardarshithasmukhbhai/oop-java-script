/********************HELLO java script object******************/
/*step 1:define the variable name 
  step 2:then after the caliy braces({}) 
  	ex.let a={
	
  }
  step 3:then after object variable will be declare
  	ex.let a={
		title
  	}
  step 4:declare variable then after ( : )
  	ex.let a={
		title:'gajjar'
  	}
  step 5:then after seprate two property name through (,)
  	ex.let a={
		title:'gajajr',
		father_name:'hasmukhbhai',
		mother_name:'anandiben'
  	}
*/

let a={
	title:'gajjar darshit',
	subjetct:'information technolgy',
	college:'goverment engineering college bhavnagar'
	  }
console.log(`i am ${a.title}.i study in ${a.college} and my subject is ${a.subject}`)

let cd={
	title:'gajjar',
	name:'hasmukhbhai',
	father_name:'babulal'
}
console.log(`hi ${cd.title} ${cd.name} and my fater's name is ${cd.father_name}`);

